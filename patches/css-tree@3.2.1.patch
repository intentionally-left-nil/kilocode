diff --git a/lib/data-patch.js b/lib/data-patch.js
index cd49cbec3e7de20dea750e95e007dbdb1488ac96..aedb22ffeb0a06f591dd1ccf0ac4e31e6c444fdc 100644
--- a/lib/data-patch.js
+++ b/lib/data-patch.js
@@ -1,6 +1,5 @@
-import { createRequire } from 'module';
-
-const require = createRequire(import.meta.url);
-const patch = require('../data/patch.json');
+// Bundlers can't follow createRequire(); use a static JSON import so the
+// data is inlined at bundle time (fixes bun build --compile binaries).
+import patch from '../data/patch.json' with { type: 'json' };
 
 export default patch;
diff --git a/lib/data.js b/lib/data.js
index 162fa2068f45413e415e22ad1fe56f4c372623ad..b73344b2187644a507265b0ecb699bbcc3738e52 100755
--- a/lib/data.js
+++ b/lib/data.js
@@ -1,10 +1,9 @@
-import { createRequire } from 'module';
+// Bundlers can't follow createRequire(); use static JSON imports so the
+// data is inlined at bundle time (fixes bun build --compile binaries).
 import patch from './data-patch.js';
-
-const require = createRequire(import.meta.url);
-const mdnAtrules = require('mdn-data/css/at-rules.json');
-const mdnProperties = require('mdn-data/css/properties.json');
-const mdnSyntaxes = require('mdn-data/css/syntaxes.json');
+import mdnAtrules from 'mdn-data/css/at-rules.json' with { type: 'json' };
+import mdnProperties from 'mdn-data/css/properties.json' with { type: 'json' };
+import mdnSyntaxes from 'mdn-data/css/syntaxes.json' with { type: 'json' };
 
 const hasOwn = Object.hasOwn || ((object, property) => Object.prototype.hasOwnProperty.call(object, property));
 const extendSyntax = /^\s*\|\s*/;
diff --git a/lib/version.js b/lib/version.js
index dde3a83f43883ecbad6ca0a23afb58c12235148a..f441e03797844149d447ed055ec1c19e9d9ae581 100644
--- a/lib/version.js
+++ b/lib/version.js
@@ -1,5 +1,5 @@
-import { createRequire } from 'module';
+// Bundlers can't follow createRequire(); use a static JSON import so the
+// data is inlined at bundle time (fixes bun build --compile binaries).
+import pkg from '../package.json' with { type: 'json' };
 
-const require = createRequire(import.meta.url);
-
-export const { version } = require('../package.json');
+export const { version } = pkg;
